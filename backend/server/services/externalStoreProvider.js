export async function getNearbyStoreCandidates(_input) {
  // Adapter point for future providers (Google Places, Foursquare, supermarket APIs, etc.).
  // Return shape should be normalized before merging into main search results.
  return []
}

